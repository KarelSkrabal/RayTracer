<UserControl x:Class="RayTracerApp.View.SphereParameters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RayTracerApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Left">
            <ItemsControl ItemsSource="{Binding Path=objects}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Sphere"/>
                            <TextBox Text="{Binding Path=Radius, Mode=TwoWay}" Width = "100" Margin = "3 5 3 5"/>
                            <TextBox Text="{Binding Path=X, Mode=TwoWay}" Width = "100" Margin = "3 5 3 5"/>
                            <TextBox Text="{Binding Path=Y, Mode=TwoWay}" Width = "100" Margin = "3 5 3 5"/>
                            <TextBox Text="{Binding Path=Z, Mode=TwoWay}" Width = "100" Margin = "3 5 3 5"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
    </Grid>
</UserControl>
